{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": {
      "backend": "cd src/backend/OpenStickyMemos.Api && dotnet restore && dotnet publish -c Release -o /app/backend",
      "frontend": "cd src/web/open-sticky-memos && npm install --legacy-peer-deps && npx ng build --configuration production"
    },
    "watchPatterns": [
      "src/backend/**",
      "src/web/**"
    ]
  },
  "deploy": {
    "numReplicas": 1,
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
